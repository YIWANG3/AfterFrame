# 代码组织结构 / 代码质量评估（2026-09-16）

配套文件：[2026-09-16-全局代码审查.md](2026-09-16-全局代码审查.md)（正确性缺陷）。这一篇只谈结构与
可维护性，不重复那 9 条 bug。

数据口径：自有代码约 **5.6 万行**（排除 `node_modules` / `.venv`）——
渲染层 34.1k、Electron 主进程 7.4k、Python sidecar 14.7k、Swift native 1.4k。

---

## 先说结论

这是一个**被认真维护过**的代码库，不是那种越滚越乱的个人项目。几个明确的优点：

- **注释质量罕见地高**。密度 7–8%，而且绝大多数是「为什么」而不是「做了什么」——
  `transport.js` 里为什么 secrets 不能走 argv、`main.js` 里为什么透明窗口要用 simple fullscreen、
  `browse.py` 里为什么 person_group 要用 `IN` 而不是相关子查询、`useJobs.js` 里为什么 bridge 要用
  ref。这些是新人（和未来的自己）最贵的信息，一般项目里根本不存在。
- **重构是真的做了，不是喊口号**。`db.py` 拆成 `db/` 包（8 个域模块）、`main.js` 抽出
  `sidecar/transport.js` + `sidecar/commands.js` + 12 个 `ipc/*.js`、编辑器抽出
  `editor/state/`（10 个 hook）+ `editor/render/`（7 个纯函数模块）。而且每次拆分都在文件头注明了
  出处（「review P3-5」「review P3-7」），可追溯。
- **纯逻辑已经被挤出 UI**。`imageMath` / `cropMath` / `splitMath` / `canvasHelpers` / `frameRender` /
  `textState` 都是无 React 依赖的纯函数，并且**有单测**。编辑器这种最容易变成一坨的地方反而是
  分层最干净的地方。
- **并发/一致性这类难点是有意识设计的，不是撞运气**。catalog generation 计数防跨库写、
  jobs 的 cooperative cancel/pause + resume cursor、settings 的 per-catalog 写队列 + 原子
  rename、预览的 `validate_source_unchanged`——这些都有明确的失败模型。
- **CI 有内容**：lint + 三套单测 + build + `npm audit`，外加 19 个 Electron E2E。

下面是问题，按「改了最省事」排序。

---

## 1. 结构性成本：一个字段要穿过 8 层、4 套命名

加一个「读写 catalog」的功能，调用链是：

```
组件 → api/index.js（门面，~140 个方法）
     → api/client.js invoke
     → preload.js（154 个 ipcRenderer 绑定）
     → ipcMain.handle（134 个 handler，散在 main.js + ipc/*.js）
     → sidecar/commands.js（57 个动词，唯一拼 argv 的地方）
     → sidecar/transport.js（常驻 JSON / one-shot argv）
     → cli.py（490 行 build_parser + 86 个 _cmd_* + 分发表）
     → db/*.py
```

而且同一件事有 **4 个名字**，彼此不自动对齐：

| 层 | 名字 |
|---|---|
| api / preload | `browseImages` |
| IPC channel | `workspace:browse` ← 已经和其它层不同名了 |
| commands 动词 | `browseImages` |
| CLI 子命令 | `browse-images` |

同时还有两个**平行实现**覆盖同一套语义：

- `api/browser/bridge.js`（1206 行）——web 版把这 ~140 个方法在浏览器里重新实现一遍；
- `mcp/server.js`（34 个工具）——给 agent 的第三套入口。

代价可量化：切图工具那个 PR 动了 **22 个文件**，而它甚至**不需要**新增 CLI 命令（复用了主进程的
`processAndSave`）。一个真正落库的功能还要再加 cli parser + handler + db 函数 + commands 动词 +
ipc handler + preload + api/index + browser bridge + MCP tool，约 9 个文件。

**这不是「设计错了」**——Electron 的进程隔离 + Python sidecar + 一个 web 变体 + 一个 agent 入口，
这四个约束确实存在。但有几个地方可以低成本收口：

- **[小] preload + api/index 合并成一张表**。两者已经是 1:1 机械映射（`api/index.js` 文件头自己
  这么写的）。用一个 `channels.js` 声明 `{ method, channel }`，preload 和 api 各自遍历生成，
  少一层手工同步、少一类「加了 preload 忘了加 api」的 bug。
- **[中] `workspace:*` channel 名从 CLI 子命令派生**，消掉 `workspace:browse` vs `browse-images`
  这种漂移。
- **[中] E2E `26-mcp-parity.spec.js` 已经在守 MCP↔IPC 的对齐，但 web bridge 没有对应的守卫**。
  `e2e-web/` 只有 3 个 spec。bridge.js 和桌面 API 漂移了不会有任何东西报警。至少加一条
  「两边导出的方法名集合必须一致」的单测——这是纯静态比对，几十行。

## 2. 拆了一半的 god file

`db.py` 和编辑器拆得很好，但另外几个还是原样：

| 文件 | 行数 | 说明 |
|---|---|---|
| `services/sidecar/.../cli.py` | 2179 | 490 行 `build_parser` + 86 个 `_cmd_*` + 分发表，全在一个文件 |
| `apps/desktop/electron/main.js` | 1846 | 见下 |
| `apps/desktop/src/components/editor/TextPanel.jsx` | 1697 | 单个面板组件 |
| `apps/desktop/electron/mcp/server.js` | 1630 | 34 个工具定义塞在一个数组字面量里 |
| `apps/desktop/src/components/EditorOverlay.jsx` | 1426 | |
| `apps/desktop/src/App.jsx` | 1398 | |
| `apps/desktop/src/hooks/useWorkspace.js` | 1121 | 一个 hook 返回 60+ 个字段 |
| `apps/desktop/src/api/browser/bridge.js` | 1206 | |

`main.js` 尤其值得再推一轮——`ipc/` 这个模式已经建好了，只是有些域搬了、有些没搬。还留在里面的
**独立**职责：

1. **EXIF 载荷构造**（`gcd` / `toExifRational` / `toExifGpsCoordinate` / `buildExifPayload` /
   `formatExifDateTime`，约 100 行）——纯数学，零 Electron 依赖，**零测试**，而且
   `readSourceMetadataForExport` 还在用 `runPythonJson` 内联一段 Python 源码字符串（打包态
   `python3` 未必存在，代码里自己也承认）。搬去 `electron/exif.js` 并补单测，收益立竿见影。
2. **token 加解密**（safeStorage + legacy 明文迁移）→ `electron/tokens.js`
3. **media:// 白名单 + 协议处理 + 视频 HTTP server** → `electron/media/`
4. **catalog 生命周期**（resolve / normalize / create / switch / prepare / sample）→ `electron/catalog.js`
5. **任务启动器**（import / enrichment / preview / ai_repaint / text_image + `formatJobStatus`）→ `electron/tasks.js`
6. **窗口 + 菜单 + 全屏**、**原生拖出**、**外部 open-file 队列**、**handwriting 缓存 LRU**

顺带一提，注入规模已经偏大了：`createMcpServer` 收 **20 个依赖**，`aiIpc.register` 15 个、
`jobsIpc` / `assetsIpc` 13 个。这既是拆分做得好的证据（有清晰的 seam，可测），也是信号：
`formatJobStatus` / `latestJobStatus` / `createJob` 被传给 5 个模块，它们本来就该是一个
`tasks` 模块的导出，而不是从 `main.js` 往下发的参数。

## 3. 抽象漏了：单一出口被绕过 + 复制粘贴

- **`sidecar/commands.js` 的文件头写着「唯一拼 argv 的地方 …… 两个入口曾经各拼各的、已经漂移过
  （docs/review-2026-06.md P3-3）」，但现在又有 10 处直接调 `callSidecarAsync` / `callSidecarJsonAsync`
  绕过它**：`summary`(×2)、`catalog-roots`、`list-tags`、`get-annotation`、`clear-ai-location`、
  `list-repaint-history`、`collage-sources`、`get-provider-token`、`split-shared-assets`、
  `repair-resource-sets`。同一个坑正在重新长出来。补齐这 10 个动词、然后**不再把
  `callSidecarJsonAsync` 传给 ipc 模块**（只传 `commands`），这条约束才是可执行的。
- **`panelBoundaries` 在两个进程里各写了一份**（`ipc/saveFile.js` 和 `editor/splitMath.js`）。
  这是无缝切图的核心不变量——两边一旦漂移，切出来的图就有缝或重叠，而且只有 E2E 能发现。
  应该只留 `splitMath.js` 一份，主进程 `require` 过去（纯函数，没有 React 依赖，可以跨进程共享）。
- **`hexToRgba` 有 4 份**（`canvasHelpers.js` 是规范版，`TextPanel.jsx` / `TextCanvas.jsx` /
  `ColorPickerPopover.jsx` 各自又写了一遍），而且**已经不一致**：TextCanvas 那份处理
  `"transparent"`，ColorPicker 那份输出带空格。改主题色相关逻辑时很容易只改一处。

## 4. 质量债：77 条 eslint 抑制，其中 42 条是死代码

`eslint-suppressions.json` 覆盖 21 个文件：

- **`no-unused-vars` × 42** —— 这些不是误报，是**真死代码**：`EditorOverlay.jsx` 里的
  `executeSave` / `beginRotate` / `pointerPoint` / `resizeCropRect` / `canvasToBlob` /
  `getTextLayers`，`AiRepaintPanel.jsx` 里的 `PROVIDER_TYPES` / `generateInstanceId` /
  `generateInstanceName`，`App.jsx` 里的 `person_group` / `selectedIdSet` / `moveSelection`，
  以及一堆没用上的 lucide 图标。**直接删**，改完 eslint 自己会挡住回潮。这是这份清单里
  投入产出比最高的一条。
- **`react-hooks/exhaustive-deps` × 35** —— 这个不能一把梭。它是 stale-closure 类 bug 的
  温床（本次 review 里 `useJobs` 的 bridge-ref 模式、`useWorkspace` 的 `jobsBridgeRef.browseContext`
  都是在绕它）。建议按文件逐个消化，每消掉一个就从抑制表里删一行——抑制表本身就是很好的进度条。

另外：**Python 侧没有任何 lint / 类型检查**。14.7k 行、类型标注已经写得相当完整（`from __future__
import annotations` + 全量 signature），却没有 ruff、没有 mypy，CI 里也只跑 unittest。
加一行 `ruff check` + 一行 `mypy --ignore-missing-imports` 是半小时的事，能直接接住本次 review 里
`catalogHasDb` 那一类的同构错误（Python 版就是「忘了调用」「拼错 kwarg」）。

## 5. 测试金字塔是倒的

| 层 | 数量 | 覆盖对象 |
|---|---|---|
| E2E（Playwright） | 38 spec | 几乎所有功能 |
| 渲染层单测（vitest） | 13 文件 / 82 case | 只有编辑器纯数学 + format + 地图 antimeridian |
| 主进程单测（node:test） | 4 文件 / 12 case | settingsStore、transport、sampleCatalog、（本次新增）saveFile |
| Python 单测 | 19 文件 / 93 case | 覆盖较好 |

问题不在总量，在**分布**：

- `useWorkspace.js`（1121 行，管着 browse 分页 / 选中 / 请求竞态 / catalog 事件 / 乐观更新）
  **一条单测都没有**，只能靠 E2E。本次 review 里 `filteredItems` 那条 bug 正好长在这里——
  它是纯函数，本来一条断言就能挡住。
- 主进程的图像/EXIF 数学（`buildExifPayload`、`toExifGpsCoordinate`、`pileSafeDragIcon`）同样
  零测试，而 `buildRegionPipeline` 的方位 bug 就是这类「纯数学、无 UI、错了肉眼很难发现」的
  典型——加了穷举单测之后 128 种组合一次就暴露了 32 种错误。
- **CI 只跑 19/38 个 E2E**（workflow 里注明了原因：GitHub macOS runner 没 GPU、AVFoundation 不稳、
  fs-watch 时序飘）。理由成立，但意味着**另一半 E2E 只在有人记得跑 `npm run e2e:only` 时才跑**。
  建议至少加一个 nightly / release-tag 触发的完整 E2E job，别让它只靠人的记性。

**建议的下一步**：把纯逻辑从 `useWorkspace` 里挤出去（`browseScopeKey`、`filteredItems` 的谓词、
`loadBrowser` 的「哪个选中还有效」决策、`revealRelatedAsset` 的 `resetScope` 判定），放进
`hooks/workspaceLogic.js` 加单测。这和编辑器当初把数学挤进 `imageMath` / `cropMath` 是同一招，
那一招在这个库里已经证明有效。

## 6. 零散的

- **没有 TypeScript，也几乎没有 JSDoc**（全仓 `@param`/`@returns` 只有 16 处）。5.6 万行 JS 全靠
  命名和注释维持契约。不建议现在整体迁移 TS（成本太高），但**跨进程边界**（preload 契约、
  IPC payload、MCP 工具的 inputSchema ↔ handler 实参）是最值得先补 JSDoc 或 `.d.ts` 的地方——
  这正是编译器帮不上忙、最容易悄悄漂移的三条缝。
- **`docs/` 有 37 个文件，大部分是 `*-plan.md`**。计划文档写得很好，但完工之后没有归档机制，
  新人分不清哪些是「现状的权威描述」（`agent-native-mcp.md`、`image-asset-model.md`）哪些是
  「已完成的历史计划」（`foundation-refactor-plan.md`、`mcp-parity-plan.md`）。建议加
  `docs/archive/`，或者在每个 plan 顶部加一行状态标记。
- **`site/`、`marketing/`、`research/`、`RESOURCES/`、`tmp/`、`test-results/` 和代码同级**。
  仓库根目录有 27 个条目，其中只有 4 个是代码。不影响构建，但第一眼很难看出这是什么项目。

---

## 优先级建议

| 优先级 | 事项 | 成本 |
|---|---|---|
| 1 | 删掉 42 处 `no-unused-vars` 死代码 | 半天，纯删除 |
| 2 | Python 侧接入 `ruff` + `mypy` 并进 CI | 半天 |
| 3 | `panelBoundaries` / `hexToRgba` 去重，补齐 10 个绕过 `commands.js` 的动词 | 1 天 |
| 4 | 把 EXIF 数学 + 任务启动器 + catalog 生命周期从 `main.js` 拆出（`ipc/` 模式已在） | 2–3 天 |
| 5 | `useWorkspace` 纯逻辑外提 + 单测；web bridge ↔ 桌面 API 方法名一致性单测 | 2–3 天 |
| 6 | 逐个消化 35 条 `exhaustive-deps` 抑制 | 持续 |
| 7 | 完整 E2E 的 nightly job；`docs/archive/` | 半天 |

`cli.py` / `TextPanel.jsx` / `mcp/server.js` 这几个大文件**暂时可以不动**——它们内部结构其实是
清楚的（分发表 / 工具数组），拆分主要是审美收益，排在上面几项后面。

---

## 进展（2026-09-16，同日落地，main 上每项一个 commit）

| # | 事项 | 结果 |
|---|---|---|
| 1 | 删 42 处 `no-unused-vars` 死代码 | ✅ 全部删除，规则恢复为硬错误；`ignoreRestSiblings` 打开（`{ drop, ...rest }` 不是疏忽） |
| 2 | Python ruff + mypy 进 CI | ✅ ruff 全绿（237 → 0，含 3 处散落在文件中部的 import、一个重复常量）；mypy 35/38 模块受检，`job_runner` / `metadata` / `reverse_lookup` 在 `pyproject.toml` 的**只减不增**排除表里（~90 条注解卫生，非 bug）。`npm run lint` = ESLint + ruff + mypy |
| 3 | 去重 + 补齐动词层 | ✅ `panelBoundaries` 一份（`shared/splitGeometry.mjs`，主进程 `require(esm)`）；`hexToRgba` 一份（吸收了 `"transparent"` 守卫）；**18 处**绕过 `commands.js` 的调用全部收口，transport 不再注入任何 ipc/mcp 模块，约束变成结构性的。未收口：job **启动**的 argv 仍在 `tasks.js` / `ipc/annotation.js` / `ipc/people.js` 各自拼（那是 `transport.launchJob` 的缝，另开一项） |
| 4 | 拆 `main.js` | ✅ `exif.js`（纯数学 + 5 组单测）、`imageMetadata.js`、`tasks.js`；1846 → 1352 行。**未做**：catalog 生命周期（`currentCatalogPath` 可变状态，~40 处读点），单独一项 |
| 5 | `useWorkspace` 纯逻辑外提 + API 面一致性测试 | ✅ `hooks/workspaceLogic.js` 四个纯函数 + 14 条单测（把「本地投影必须是 `_search_clause` 超集」钉死）；`api/apiSurface.test.js` 三方文本比对，**首次运行就抓到 4 个从未进门面的 preload 方法**，已补。剩余债：14 个文件 65 处直接 `window.mediaWorkspace.*`，机械迁移，另开一项 |
| 6 | `exhaustive-deps` 逐条消化 | 🟡 第一片：`AnnotationsSection` 四个 callback 补 `t`；`usePeopleGroups` 的 `run` 改 `useCallback`、5 条行内 disable 归零；`useWorkspace` 五个「只响应单一输入」的 effect 改为带理由的行内 disable。静默抑制 **35 → 26**（8 文件：`App.jsx` 3、`EditorOverlay` 7、`CollageCanvas` 6、`CollageOverlay` 2、`Lightbox` 2、`AiRepaintPanel` 2、`TextCanvas` 2、`TextPanel` 2）。另有 26 条是已带行内理由的有意为之，不算债 |
| 7 | nightly 全量 E2E + docs 归档 | ✅ `.github/workflows/e2e-nightly.yml`（每日 + 手动触发，全部 38 个 spec，失败上传诊断）；`docs/README.md` 把 30 篇文档分成 现状权威 / 进行中 / 已完成计划 / 已撤回，不搬文件（代码注释有直链） |

顺带修的：`people-recognition-design.md` 文内状态行仍写「尚未实现」，与实际不符，已在索引里标注，待回填。

## 后续（每项一个 PR）

| PR | 事项 | 结果 |
|---|---|---|
| #45 | 上表 1–7 | ✅ 合入 |
| #46 | 第 5 项遗留：58 处 `window.mediaWorkspace.*` 迁到 `api.*` | ✅ ESLint `no-restricted-syntax` 把门面变成硬约束（`src/api/**` 与 `web-main.jsx` 之外一律禁止） |
| #47 | 第 4 项遗留：catalog 生命周期出 `main.js` | ✅ `electron/catalog.js`（`createCatalogState`，`path()` / `state()` / `set()` / `hasDb()`）+ 单测；`main.js` 1352 → 1279 行 |
| #48 | stdout 管道关闭后 EPIPE 变「Uncaught Exception」弹窗 | ✅ 修 bug 时发现；stream `error` 监听吞 EPIPE |
| #49 | E2E：透明窗口上 CDP 元素截图闪烁 → `capturePage` 裁剪；编辑器后门装好前就驱动 → `waitForEditor` | ✅ 本地全量 170/170 |
| #51 | 第 3 项遗留：job **启动** argv 收口 | ✅ `electron/sidecar/jobArgv.js` 七个纯函数（enrichment / import / preview / ai-repaint / text-image / annotation / people-index），`tasks.js` / `ipc/annotation.js` / `ipc/people.js` 不再自己拼；`jobArgv.test.js` 文本解析 `cli.py` 的 argparse，钉死「发出的每个 flag 都存在、required 的都发了、没有未覆盖的 `run-*-job`」 |
| #52 | 第 6 项第二片：编辑器一组 13 条静默 `exhaustive-deps` 归零 | ✅ `EditorOverlay` 7（标尺常量提到模块级；两个绘制 effect 改按「有没有」而非对象身份依赖；测试后门改为每次渲染重建方法表、按 `open` 装一次；快捷键改 `useEffectEvent`）、`AiRepaintPanel` 2、`TextPanel` 2 都用 React 19.2 的 `useEffectEvent`；`TextCanvas` 2 是缓存失效键，改为带理由的行内声明。顺带修：`App.jsx` 的后门 effect 原来整个替换 `window.__afterframeTest`，子 effect 先跑，编辑器刚装好的方法会被父组件覆盖，改为合并 |
| #53 | 第 6 项第三片：拼图一组 8 条归零 | ✅ `CollageCanvas` 6：纯函数 `getPreviewCandidates` / `isReady` 提到模块级；`redraw` / `scheduleRedraw` 变成稳定句柄（经 ref 派发到最新的 `paint`），图片 `onload` 也不再调用过期的绘制闭包；`emitSelectedState` / `getLoadedImage` 改 `useCallback`；选区钳制改函数式 `setSelectedIdx`。`CollageOverlay` 2：换源重置分页、按数量自动选模板都改 `useEffectEvent`（前者若把 `loadPage` 列进依赖，每页落地都会触发重置） |
| #54 | 第 6 项收尾：`App` 3 + `Lightbox` 2 归零，**`eslint-suppressions.json` 删除** | ✅ `App.jsx`：任务完成回调、监视目录补扫、全局快捷键三个 effect 都改 `useEffectEvent`（快捷键监听器从「每次状态变化重绑」变成挂载绑一次），`shouldIgnoreKey` 提为模块函数。`Lightbox`：resize observer 与滚轮监听通过 `useEffectEvent(applyView)` 调最新的 `applyView`。第 6 项从 77 条抑制（42 死代码 + 35 静默 deps）走到 0，`react-hooks/exhaustive-deps` 现在是无豁免的硬错误 |
| #54 附带 | CI 上反复出现的 19-editor-layers undo 失败 | ✅ `waitForEditor({ preview: true })` 只等预览解码、不等初始快照；慢 runner 上首次编辑落在 history 为空时，undo 在 index 0 no-op。新增 `getEditReady`，spec 断言 `historyLength > 0` |
| #55 | E2E 覆盖率度量（§5「金字塔是倒的」终于有数字） | ✅ `npm run e2e:coverage`：renderer istanbul + main `NODE_V8_COVERAGE` + sidecar Python `coverage`，一份脚本出三份报告；nightly 跑这个变体并上传 HTML。首次全量：renderer **62%** / main **75%** / sidecar **55%**（行），冷点与建议见 [2026-09-17-E2E覆盖率.md](2026-09-17-E2E覆盖率.md) |
| #56 | 覆盖清单 A1：RAW 导入与配对 e2e | ✅ `35-raw-import.spec.js` 六条，进 CI 子集；fixture 是两张真实 DNG 缩到 1024px（各 ~200 KB，`fixtures/raw/make-raw-fixtures.py` 可复现）。**抓到 bug**：MCP `raw_pairing confirm_match` 从未成功过（sidecar 打印文本、Electron 解析 JSON），已改为 JSON。另：detached job runner 的 argv 现在记一行 `[sidecar:job]` 日志，之前 stdio ignore 后什么都看不到 |
| #57 | 覆盖清单 A2 / A3 / A4：MCP 工具与后台 job | ✅ `36-mcp-jobs-tools.spec.js` 七条，进 CI 子集：`get_catalog_info` / `view_assets` / `generate_previews` / `delete_assets` / `pause_job` / `cancel_job` / `resume_job` / `index_people`（无模型引导报错）首次被调用；preview job runner 首次跑通（fixture 启动时剥掉全部 HD 预览，让它真有活干）。**CI 暴露的两个问题顺带修了**：种子 catalog 存的是构建机器的绝对路径，CI 上每张图都是「Missing」、凡是要碰原图的操作都不可能成功（之前被随 fixture 提交的预览图掩盖），`launchApp` 现在启动前把路径前缀重定位到当前 checkout；`assets` 范围的 catalog-changed 广播只刷画廊不刷 summary，agent 删照片后侧栏「All Assets N」不变 |
| #59 | 覆盖清单 A5 + A6：HEIC 导入、旧 schema 迁移 | ✅ `37-heic-import`（2 条）、`38-legacy-catalog`（1 条）。**抓到 bug**：从没有 `asset_files` 的旧库升级后表是空的，summary 计 0、侧栏「No indexed assets yet」而画廊有内容；`_apply_latest_schema` 现在回填一行 primary 文件（单测 + e2e 各钉一条） |
| #60 | 覆盖清单 B1 + B2：右键菜单、选区模型 | ✅ `39-context-menu-selection.spec.js` 七条进 CI 子集；37 / 38 也补进子集（35 个 spec 文件） |
| #61 | 覆盖清单 B4：裁剪框鼠标操作 | ✅ `40-crop-mouse.spec.js` 七条进 CI 子集；给手柄和角度标尺加了 `data-crop-handle` / `data-testid` 标记 |
| #62 | 三项遗留小债一次收掉 | ✅ mypy：三个模块 91 条归零，`pyproject.toml` 的豁免表删除，根因是 JSON 形状的字典被标成 `dict[str, object]`（应为 `dict[str, Any]`），其余是几处重定义和三个返回元组的 checkpoint lambda；`people-recognition-design.md` 状态行回填；`useTextTool` 在图层栈变化时剔除已不存在的选中 id（undo 撤掉新增图层后 Inspector 曾指向幽灵图层），19 里加了断言 |
| #63 | 覆盖清单 B5：文字层手势 + TextPanel | ✅ `41-text-layer-panel.spec.js` 七条进 CI 子集；**抓到 bug**：画布 shift 点击多选被 `startDrag` 重置成单选，已修 |
| #64 | 覆盖清单 B3 + B9：拖放导入、原生菜单 | ✅ `42-drop-and-menu.spec.js` 六条进 CI 子集；main.js 里 16 个菜单 `click` 从此有 e2e |
| #65 | 覆盖清单 B6：贴纸库 | ✅ `43-sticker-library.spec.js` 四条进 CI 子集（和 04 一样按 Xcode 工具链门控） |
| #66 | 覆盖清单 B7：Discover 视图 | ✅ `44-discover.spec.js` 五条进 CI 子集 |
| #68 | 44 在 CI 上挂：Rated 切回 All Assets 没生效；02 本地复现 Rated 停在 14 张 | ✅ 两个方向是同一个竞态：`refreshAll` 先等六个状态 IPC 再浏览，用的是**调用时**的 scope，所以先点的 Recently Added 可能比后点的 Rated 晚落地并覆盖它；job 完成后的刷新拿的也是旧闭包的 scope；反向则是 `setStatusFilter` 的浏览不带 `force` 被忙碌守卫丢掉。修法：`loadBrowser` / `refreshAll` 默认 scope 读桥接 ref（最新一次渲染），显式切 scope 的入口都 bump 一个 intent 计数，`refreshAll` 等完 IPC 发现 intent 变了就改用最新 scope；`setStatusFilter` / `selectCollection` 加 `force`。02 回到 CI 子集。顺手把 40 的导出用例改成等 cropRect 真正成方再导出（VM 上差 80 px） |
| #67 | 覆盖清单 B8：拼图单页 | ✅ `45-collage-single.spec.js` 五条进 CI 子集；40 的导出用例加了「等裁剪框真正变方」的等待（CI 上慢一拍导出了旧框）；顺手修 40 导出用例在 CI 上的假阴性：`getState().cropRect` 是同步更新的 ref，导出走的是最新一次渲染的 `normalizedCrop`，VM 上渲染慢一拍就拿上一条用例的旧裁剪导出（300×220，差 80 px）。后门多暴露 `cropRectRendered`，用例等它成方再导 |
| #70 | Stickers 页面卡片右键没反应（用户手动确认） | ✅ 43 里留的「待手动确认」是真 bug：打开菜单的 contextmenu 事件还在冒泡时菜单已挂载，其 outside-click 监听器收到同一个事件立刻关掉（挂载到卸载 0.2 ms）。`StickerContextMenu` 忽略早于自身挂载的事件；43 改回真实右键 |
| 本次 | 05 的缩略图自愈用例从第一次度量起就挂，一直挂着没查 | ✅ 是真 bug：画廊的两条修复触发（`<img>` 加载失败、浏览响应里的 source_changed / 元数据缺失）共用一份「每资产两次」额度。打开目录时后者的批量自愈立刻把额度花光，此后同一张图的缩略图再坏就永远不修。额度改为按触发原因分开计数，并在资产恢复健康（图片加载成功 / 陈旧信号消失）时归还；用例里直写 sqlite 的那句补 busy timeout。05 回到 CI 子集 |

仍欠的：无。清单见 [2026-09-17-E2E覆盖率.md](2026-09-17-E2E覆盖率.md) 的 B3 / B5–B9。
